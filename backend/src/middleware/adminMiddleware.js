const adminOnly = (req, res, next) => {
  if (req.user && req.user.role === 'admin') {
    next();
  } else {
    res.status(403).json({
      success: false,
      message: 'Access denied. Director / Administrator privileges are required to perform this operation.',
    });
  }
};

export default adminOnly;
